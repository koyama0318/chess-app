// Stub for wasm-pkg/chess_wasm — only used for Vite module resolution during tests.
// Actual mocking is done via vi.mock in individual test files.
export default async function init() {}
export class ChessGame {}
export function get_legal_moves() { return []; }
